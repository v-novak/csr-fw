#include "servo_controller.h"
/*
void ServoController::setup(int pin, int zero_point, float coefficient)
{
    _servo.attach(pin);
    _zero_point = zero_point;
    _coefficient = coefficient;
    _servo.write(zero_point);
}

void ServoController::rotate(int angle)
{
    if (_coefficient == 1.0f) {
      _servo.write(angle + _zero_point);
    } else {
      _servo.write(angle * _coefficient + _zero_point); 
    }
}*/
